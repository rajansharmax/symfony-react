import React, { useContext } from "react";
import { Routes } from "react-router-dom";


const Footer = () => {
    return (
      <React.Fragment>
        <Routes>
            <h1>footer</h1>
        </Routes>
      </React.Fragment>
    );
};

export default Footer;