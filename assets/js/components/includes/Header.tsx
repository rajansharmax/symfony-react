import React, { useContext } from "react";
import { Routes } from "react-router-dom";

const Header = () => {
  return (
    <React.Fragment>
      <Routes>
        <h1>Header</h1>
      </Routes>
    </React.Fragment>
  );
};

export default Header;
