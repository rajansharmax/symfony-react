import React, { useContext } from "react";
import { Routes } from "react-router-dom";

const Sidebar = () => {
  return (
    <React.Fragment>
      <Routes>
        <h1>Sidebar</h1>
      </Routes>
    </React.Fragment>
  );
};

export default Sidebar;
